name: Build Unsigned Binaries
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-unsigned:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 90
    env:
      BUZZ_DISABLE_TELEMETRY: true
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
          - os: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Downgrade Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.0.0'
        if: matrix.os == 'macos-latest'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Vulkan SDK
        if: matrix.os == 'windows-latest'
        uses: humbletim/install-vulkan-sdk@v1.2
        with:
          version: 1.4.309.0
          cache: true

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install Inno Setup on Windows
        uses: crazy-max/ghaction-chocolatey@v3
        with:
          args: install innosetup --yes
        if: matrix.os == 'windows-latest'

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2
        if: matrix.os == 'windows-latest'

      - name: Install dependencies
        run: uv sync

      - uses: AnimMouse/setup-ffmpeg@v1
        id: setup-ffmpeg
        with:
          version: ${{ matrix.os == 'macos-latest' && '71' || '7.1' }}

      - name: Bundle
        run: |
          if [ "$RUNNER_OS" == "macOS" ]; then
            brew install create-dmg
            uv run make bundle_mac_unsigned
          elif [ "$RUNNER_OS" == "Windows" ]; then
            cp -r ./dll_backup ./buzz/
            uv run make bundle_windows
          fi
        shell: bash

      - uses: actions/upload-artifact@v4
        with:
          name: Buzz-Unsigned-${{ runner.os }}
          path: |
            dist/Buzz*-windows.exe
            dist/Buzz*-mac.dmg
